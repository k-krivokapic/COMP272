/* Complete the odd() method. This method should return an ArrayList with values of the 
 corresponding odd keys. */

public ArrayList<String> odd(HashMap<Integer, String> map) {
    ArrayList<String> result = new ArrayList<>();
    // traverse through the HashMap
    for (int i = 0; i <= map.size(); i++) {
      // if the value is odd and found in the map, add it to the result
      if (i % 2 != 0 && map.containsKey(i)) {
        result.add(map.get(i));
      }
    }
    return result;
  }
